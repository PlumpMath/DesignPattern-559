概要说明：原型模式也是“对象创建型”模式，复制原型对象然后创建新的对象实例
浅度克隆与深度克隆：
1.默认的克隆是浅度克隆，只能赋值简单类型的数据，负载类型的数据还是只有一份，共用
2.深度克隆就需要手动给对象赋值，不能return super.clone
适用情景：
1.在创建对象的时候，我们不仅希望继承类的基本结构，还希望拿到原型对象的数据
2.希望对目标对象的修改不影响原型对象(可能需要深度克隆)
3.隐藏克隆的细节